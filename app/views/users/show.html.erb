<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Color:</strong>
  <%= @user.color %>
</p>

<ul class="channels">
  <% @user.channels.each do |c| %>
    <li><%= c.name %></li>
  <% end %>
</ul>


<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
